namespace MobileAppAPI.Helpers
{
    public class SecurityConstants
    {
        public static readonly int REFRESH_TOKEN_EXPIRY = 7; //expiration for refresh token (days)
        public static readonly int ACCESS_TOKEN_EXPIRY = 1; //expiration for access token (hours)

    }
}
